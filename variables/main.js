// Constantese
/**
 * var ->   Se usa para variables globales
 *   ej: var Nombre_Variables = 'data';
 * let ->   Para variables que se ejecutan en su 
 *          bloque (Funciones - bucles ...etc)
 *   ej: let Nombre_Variables = 'data';
 * const -> Variables con no van a cambiar en el flojo
 *          del algoritmo
 *   ej:const Nombre_Variables = 'data';
 */

var numeroUno = 1;
var numeroDos = true;

// console.log nos ayuda a visualizar información en la consola del navegador
// para acceder lo hacemos usando la tecla f12
console.log(numeroUno);
console.log(numeroDos);

const DATO_TRES = 'Hola mundo';
console.log(DATO_TRES);

let numeroCuatro = 3;
console.log(numeroCuatro);
 